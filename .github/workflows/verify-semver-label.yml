name: Verify Semver Label

# Every pull request declares release intent explicitly: major/minor/patch publishes, while no-release states that
# the change affects only internal tooling, specs, documentation, or automation. Omitting a label is never an
# implicit no-release because it is indistinguishable from forgetting to label a consumer-facing change.
#
# Triggered on labeled/unlabeled as well as the usual events, so adding the label re-runs the check rather than
# leaving a red cross behind that only a push would clear.
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

on:
  pull_request:
    types: [opened, reopened, synchronize, labeled, unlabeled]
    branches:
      - main

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest

    steps:
      - name: Require exactly one release intent label
        env:
          LABELS: ${{ toJSON(github.event.pull_request.labels.*.name) }}
        run: |
          count=$(printf '%s' "$LABELS" | jq '[.[] | select(. == "major" or . == "minor" or . == "patch" or . == "no-release")] | length')

          if [ "$count" -eq 1 ]; then
            echo "Found one release intent label."
            exit 0
          fi

          if [ "$count" -eq 0 ]; then
            echo "::error::This pull request has no release intent label. Add exactly one of major, minor, patch, or no-release."
          else
            echo "::error::This pull request carries $count release intent labels. Exactly one of major, minor, patch, or no-release is required."
          fi

          exit 1
